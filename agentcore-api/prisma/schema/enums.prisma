enum UserRole {
  super_admin
  org_admin
  product_admin
  agent
  user
}

enum OrganizationStatus {
  active
  inactive
  suspended
}

enum OrganizationPlan {
  free
  starter
  pro
  enterprise
}

enum DeploymentMode {
  local
  saas
}

enum ProductKey {
  customer_chat
  appointment_booking
  whatsapp_assistant
  voice_receptionist
}

enum ProductStatus {
  active
  inactive
}

enum OrganizationProductStatus {
  enabled
  disabled
}

enum AIProviderType {
  openai
  anthropic
  local
  custom
}

enum AIProviderStatus {
  active
  inactive
}

enum KnowledgeSourceType {
  website_url
  uploaded_file
  text
  faq
}

enum KnowledgeSourceStatus {
  pending
  processing
  ready
  failed
}

enum StorageProvider {
  s3
  r2
  minio
}

enum CustomerChatConversationStatus {
  open
  waiting_for_agent
  closed
}

enum CustomerChatMessageRole {
  visitor
  assistant
  agent
  system
}

enum AppointmentServiceStatus {
  active
  inactive
}

enum AppointmentStaffStatus {
  active
  inactive
}

enum AppointmentResourceStatus {
  active
  inactive
}

enum AppointmentBookingStatus {
  pending
  confirmed
  cancelled
  completed
  no_show
}

enum AppointmentReminderStatus {
  pending
  processing
  sent
  failed
  cancelled
  skipped
}

enum AppointmentCalendarProvider {
  google
  microsoft
}

enum AppointmentCalendarConnectionStatus {
  pending
  active
  error
  disconnected
}

enum AppointmentCalendarEventStatus {
  pending
  syncing
  synced
  failed
  deleted
}

enum WhatsAppProviderType {
  meta
  twilio
  custom
}

enum WhatsAppConfigStatus {
  active
  inactive
}

enum WhatsAppConversationStatus {
  open
  waiting_for_agent
  closed
}

enum WhatsAppMessageDirection {
  inbound
  outbound
}

enum WhatsAppMessageRole {
  contact
  assistant
  agent
  system
}

enum WhatsAppMessageType {
  text
  image
  audio
  video
  document
  sticker
  location
  unknown
}

enum VoiceProviderType {
  twilio
  sip
  custom
}

enum VoiceConfigStatus {
  active
  inactive
}

enum VoiceCallStatus {
  ringing
  in_progress
  waiting_for_agent
  transferred
  voicemail
  completed
  failed
}

enum VoiceCallEventType {
  call_started
  stt_partial
  transcript
  assistant_response
  tts_started
  barge_in
  route_decision
  transfer_requested
  voicemail
  call_ended
  system
}
